<!doctype html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,minimum-scale=1.0,maximum-scale=10.0,initial-scale=1.0" />
    <meta http-equiv='X-UA-Compatible' content='IE=edge;chrome=1' />    
    <%= javascript_include_tag 'modernizr' %>
    <%= stylesheet_link_tag "styles" %>
  </head>
  <body>
    
    <div class="canvas">

      <header class="page-header">
        <a href="/" class="brand">Deric Mendez</a>
        <ul>
          <% data.links.social.each do |s| %>
            <li><a target="_blank" class="<%= s.className %>" href="<%= s.url %>"><span><%= s.name %></span></a></li>
          <% end %>
        </ul>
      </header>

      <div class="main" role="main">
        <%= yield %>
        
        <!--<aside>
          <h2>Recent Articles</h2>
          <ol>
            <% blog.articles[0...10].each do |article| %>
              <li><a href="<%= article.url %>"><%= article.title %></a> <span><%= article.date.strftime('%b %e') %></span></li>
            <% end %>
          </ol>

          <h2>Tags</h2>
          <ol>
            <% blog.tags.each do |tag, articles| %>
              <li><a href="<%= tag_path tag %>"><%= tag %> (<%= articles.size %>)</a></li>
            <% end %>
          </ol>

          <h2>By Year</h2>
          <ol>
            <% blog.articles.group_by {|a| a.date.year }.each do |year, articles| %>
              <li><a href="<%= blog_year_path year %>"><%= year %> (<%= articles.size %>)</a></li>
            <% end %>
          </ol>
        </aside>-->
      </div>

      <footer class="page-footer">
        &copy; 2012 &mdash; <%= link_to 'Deric Mendez', 'http://www.dericmendez.com' %>
      </footer>
    </div>


  </body>
</html>
